<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neuedu.mapper.DepartmentMapper">
    <select id="selectAll" resultType="com.neuedu.entity.Department">
        select id, dept_code, dept_name, dept_type, delmark
        from department
        where delmark = #{delmark}
    </select>

    <select id="list" resultType="Department">
        SELECT id, dept_code, dept_name, dept_type, delmark
        FROM department
        WHERE delmark = 1
    </select>

    <!-- 新增部门 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO department(dept_code, dept_name, dept_type, delmark)
        VALUES(#{deptCode},#{deptName},#{deptType},1)
    </insert>

    <!-- 逻辑删除 -->
    <update id="deleteById">
        UPDATE department
        SET delmark = 0
        WHERE id = #{id}
    </update>


</mapper>
